<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Happy Clinic Stats</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
    <%- include('partials/adminNavbar') %>
  <div class="max-w-4xl mx-auto p-8">
    <h1 class="text-3xl font-bold text-center mb-6">📊 Happy Clinic Statistics</h1>

    <form action="/admin/anylyzer" method="get" class="mb-8 flex justify-center gap-4">
      <label for="type" class="text-lg font-medium">Select Period:</label>
      <select name="type" id="type" class="p-2 rounded-lg border border-gray-300">
        <option value="day" <%= type === 'day' ? 'selected' : '' %>>Today</option>
        <option value="month" <%= type === 'month' ? 'selected' : '' %>>This Month</option>
        <option value="year" <%= type === 'year' ? 'selected' : '' %>>This Year</option>
      </select>
      <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600">Show Stats</button>
    </form>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
      <div class="bg-white rounded-xl p-6 shadow hover:shadow-md">
        <div class="text-4xl mb-2">👤</div>
        <h2 class="text-lg font-semibold mb-2">New Users</h2>
        <p class="text-2xl font-bold text-blue-600"><%= stats.newUsers %></p>
      </div>
      <div class="bg-white rounded-xl p-6 shadow hover:shadow-md">
        <div class="text-4xl mb-2">📅</div>
        <h2 class="text-lg font-semibold mb-2">Appointments</h2>
        <p class="text-2xl font-bold text-blue-600"><%= stats.newAppointments %></p>
      </div>
      <div class="bg-white rounded-xl p-6 shadow hover:shadow-md">
        <div class="text-4xl mb-2">💳</div>
        <h2 class="text-lg font-semibold mb-2">Payments</h2>
        <p class="text-gray-700 text-xl font-semibold">
            Revenue: <%= (stats.payments / 1_000_000).toFixed(2) %>M
        </p>
      </div>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
        <div class="bg-white p-4 rounded shadow">
            <img src="<%= charts.userChart %>" alt="User Chart" />
        </div>
        <div class="bg-white p-4 rounded shadow">
            <img src="<%= charts.appointmentChart %>" alt="Appointment Chart" />
        </div>
        <div class="bg-white p-4 rounded shadow">
            <img src="<%= charts.paymentChart %>" alt="Payment Chart" />
        </div>
    </div>
  </div>
</body>
</html>
