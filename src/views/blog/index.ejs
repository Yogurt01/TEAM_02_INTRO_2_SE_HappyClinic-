<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Health Blog</title>
</head>
<body>
  <h1>Blog - Health Tips and Articles</h1>
  <a href="/blog/create">Create New Post</a>
  <div>
    <% posts.forEach(post => { %>
      <div>
        <h2><%= post.title %></h2>
        <p><%= post.content %></p>
        <p><small>By <%= post.author %> on <%= post.date.toLocaleDateString() %></small></p>
      </div>
    <% }) %>
  </div>
</body>
</html>
